module top;

reg a, b;
wire y0, y1, y2;

logic_gates uut (
    .a(a),
    .b(b),
    .y0(y0),
    .y1(y1),
    .y2(y2)
);

initial begin
    a = 0; b = 0; #10;
    a = 0; b = 1; #10;
    a = 1; b = 0; #10;
    a = 1; b = 1; #10;
end

endmodule
